// Package mlir provides Go bindings built on top of the MLIR C API.
//
// The repository is at an early stage. The initial public surface focuses on
// explicit resource ownership and direct access to native MLIR handles before
// higher-level builder and dialect layers are added.
package mlir
